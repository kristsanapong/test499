<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proj499">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="Blood Bank"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TechnicianActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".TechnicianAddAccountActivity"
            android:label="เพิ่มข้อมูลผู้ใช้งาน" />
        <activity
            android:name=".History"
            android:label="@string/title_activity_history" />
        <activity
            android:name=".bloodinformation"
            android:label="@string/title_activity_bloodinformation" />
        <activity
            android:name=".FeaturesActivity"
            android:label="FeaturesActivity" />
        <activity
            android:name=".Queue"
            android:label="Queue" />
        <activity
            android:name=".blooddonation"
            android:label="Blood Donation" />
        <activity android:name=".Signup" />
        <activity
            android:name=".Profile"
            android:label="@string/title_activity_profile" />
        <activity
            android:name=".ManageDataTechnician" />
        <activity
            android:name=".EditBloodData" />
        <activity
            android:name=".MedicalActivity" />
        <activity
            android:name=".News"
            android:label="@string/title_activity_news" />
        <activity
            android:name=".Technician_Account" />
        <activity
            android:name=".Technician_News" />

        <activity
            android:name=".TechnicianAddMedicalActivity" />
        <activity
            android:name=".TechnicianDataAccountActivity" />
        <activity
            android:name=".UserProfile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>